import { Hooks } from '@rpgjs/common';
import { RpgPlayer } from '../Player/Player';
import { RpgRoomConnection } from './map';
/** Mark a successful server-authorized transfer before telling the client to reconnect. */
export declare function markRoomTransfer(connection: RpgRoomConnection, token: unknown): void;
/** Dispatch once for a closed connection, excluding deliberate room transfers. */
export declare function dispatchPlayerDisconnected(hooks: Hooks, player: RpgPlayer, connection: RpgRoomConnection | null): Promise<void>;
