import { RpgPlayer } from '../Player/Player';
export declare const GUI_ACTION_COOLDOWN_MS = 250;
export interface GuiOpenOptions {
    waitingAction?: boolean;
    blockPlayerInput?: boolean;
}
export declare class Gui {
    id: string;
    protected player: RpgPlayer;
    private static _openSequence;
    private _close;
    private _blockPlayerInput;
    private _events;
    private _closed;
    openId: string | null;
    constructor(id: string, player: RpgPlayer);
    open(data?: unknown, { waitingAction, blockPlayerInput }?: GuiOpenOptions): Promise<unknown | null>;
    matchesOpenId(openId?: unknown): boolean;
    on<T = unknown>(event: string, callback: (data: T) => unknown | Promise<unknown>): void;
    emit<TResult = unknown>(event: string, data: unknown): Promise<TResult | null>;
    close(data?: unknown): void;
    update(data?: unknown, { clientActionId }?: {
        clientActionId?: string;
    }): void;
}
