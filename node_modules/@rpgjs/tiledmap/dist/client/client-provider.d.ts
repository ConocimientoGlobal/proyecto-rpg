import { default as client } from './client';
import { TiledMapOptions } from './index';
export declare function createTiledMapClientProviders(options: TiledMapOptions): {
    client: typeof client;
    providers: any[];
};
