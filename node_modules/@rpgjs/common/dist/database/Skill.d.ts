import { RpgWritableSignal } from '../foundation';
export interface SkillData {
    id: string;
    name: string;
    description: string;
    spCost: number;
    hitRate: number;
    power: number;
    coefficient: Record<string, number>;
    icon: string;
}
export declare class Skill {
    id: RpgWritableSignal<string>;
    name: RpgWritableSignal<string>;
    description: RpgWritableSignal<string>;
    spCost: RpgWritableSignal<number>;
    icon: RpgWritableSignal<string>;
    hitRate: RpgWritableSignal<number>;
    power: RpgWritableSignal<number>;
    coefficient: RpgWritableSignal<Record<string, number>>;
    constructor(data?: SkillData);
}
