import { RpgTransportServerConstructor } from '@rpgjs/server/node';
import { ViteDevServer } from 'vite';
export interface RpgjsDevServerOptions {
    /** Remote Node or Wrangler origin. When omitted, Vite hosts the Node transport. */
    target?: string;
    /** Map ids published to the remote administration endpoint. */
    mapIds?: string[];
    mapUpdateToken?: string;
    tiledBasePaths?: string[];
    /** Build a provider-specific authoritative payload before remote publication. */
    resolveMapPayload?: (context: {
        mapId: string;
        defaultPayload: unknown;
    }) => unknown | Promise<unknown>;
}
export declare function serverPlugin(serverModule: RpgTransportServerConstructor, options?: RpgjsDevServerOptions): {
    name: string;
    config(): {
        server: {
            proxy: {
                "/parties": {
                    target: string;
                    ws: boolean;
                    changeOrigin: boolean;
                };
            };
        };
    } | undefined;
    configureServer(server: ViteDevServer): Promise<void>;
    buildStart(): void;
    buildEnd(): void;
};
