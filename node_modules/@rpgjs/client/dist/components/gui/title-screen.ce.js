import { inject } from "../../core/inject.js";
import { getKeyboardControlBind } from "../../services/actionInput.js";
import { RpgGui } from "../../Gui/Gui.js";
import { RpgClientEngine } from "../../RpgClientEngine.js";
import { DOMContainer, DOMElement, Navigation, computed, cond, effect, h, loop, mount, signal, useDefineEmits, useDefineProps, useProps } from "canvasengine";
import { PrebuiltGui } from "@rpgjs/common";
//#region src/components/gui/title-screen.ce
function component($$props) {
	useProps($$props);
	const defineProps = useDefineProps($$props);
	useDefineEmits($$props);
	const engine = inject(RpgClientEngine);
	const { t } = engine.i18n();
	const guiService = inject(RpgGui);
	const keyboardControls = engine.globalConfig.keyboardControls;
	const { data, onInteraction } = defineProps({
		entries: { default: () => [] },
		saveLoad: { default: () => ({}) },
		localActions: { default: false },
		backgroundMusic: { default: null },
		backgroundImage: { default: null }
	});
	const { entries, title, subtitle, version, saveLoad, localActions, backgroundMusic, backgroundImage } = data();
	const defaultEntries = [{
		id: "start",
		label: t("rpg.title.start")
	}, {
		id: "load",
		label: t("rpg.title.load")
	}];
	const resolveProp = (value) => typeof value === "function" ? value() : value;
	const titleText = computed(() => resolveProp(title) || t("rpg.title.default"));
	const subtitleText = computed(() => resolveProp(subtitle) || "");
	const versionText = computed(() => resolveProp(version) || "");
	const localActionsEnabled = computed(() => resolveProp(localActions) === true);
	const backgroundMusicId = computed(() => resolveProp(backgroundMusic) || engine.globalConfig?.menus?.titleScreen?.settings?.backgroundMusic || "");
	const backgroundImageSource = computed(() => resolveProp(backgroundImage) || engine.globalConfig?.menus?.titleScreen?.settings?.backgroundImage || "");
	const backgroundImageStyle = computed(() => {
		const source = backgroundImageSource();
		return source ? `url(${source})` : "none";
	});
	const entryList = computed(() => {
		const list = resolveProp(entries);
		if (Array.isArray(list) && list.length) return list;
		return defaultEntries;
	});
	const isEntryDisabled = (entry) => {
		if (!entry) return true;
		if (entry.disabled) return true;
		if (entry.enabled === false) return true;
		return false;
	};
	const selectedEntry = signal(0);
	const selectableIndexes = computed(() => {
		return entryList().map((entry, index) => isEntryDisabled(entry) ? null : index).filter((value) => value !== null);
	});
	effect(() => {
		const available = selectableIndexes();
		if (!available.length) return;
		const current = selectedEntry();
		if (!available.includes(current)) selectedEntry.set(available[0]);
	});
	const moveSelection = (delta) => {
		const available = selectableIndexes();
		if (!available.length) return;
		const current = selectedEntry();
		const nextIndex = (Math.max(0, available.indexOf(current)) + delta + available.length) % available.length;
		selectedEntry.set(available[nextIndex]);
		engine.playUiSound("navigate");
	};
	const triggerSelect = (index) => {
		const entry = entryList()[index];
		if (!entry || isEntryDisabled(entry)) {
			engine.playUiSound("error");
			return;
		}
		engine.playUiSound("confirm");
		if (localActionsEnabled()) {
			if (entry.id === "start") guiService.hide(PrebuiltGui.TitleScreen);
			if (entry.id === "load") {
				const config = resolveProp(saveLoad) || {};
				const slots = Array.isArray(config.slots) ? config.slots : [
					null,
					null,
					null
				];
				guiService.display(PrebuiltGui.Save, {
					...config,
					mode: config.mode || "load",
					slots
				});
			}
		}
		if (onInteraction) onInteraction("select", {
			id: entry.id,
			index,
			entry
		});
	};
	function selectEntry(index) {
		return function() {
			selectedEntry.set(index);
			engine.playUiSound("navigate");
			triggerSelect(index);
		};
	}
	const controls = signal({
		up: {
			repeat: true,
			bind: keyboardControls.up,
			throttle: 150,
			keyDown() {
				if (guiService.isDisplaying(PrebuiltGui.Save)) return;
				moveSelection(-1);
			}
		},
		down: {
			repeat: true,
			bind: keyboardControls.down,
			throttle: 150,
			keyDown() {
				if (guiService.isDisplaying(PrebuiltGui.Save)) return;
				moveSelection(1);
			}
		},
		action: {
			bind: getKeyboardControlBind(keyboardControls.action),
			keyDown() {
				if (guiService.isDisplaying(PrebuiltGui.Save)) return;
				triggerSelect(selectedEntry());
			}
		},
		gamepad: { enabled: true }
	});
	mount(() => {
		engine.playUiSound("open");
		const music = backgroundMusicId();
		if (music) {
			engine.music.contextId = "title-screen";
			engine.music.enter(music, {
				volume: .8,
				mapVolume: 0,
				fadeInMs: 600,
				fadeOutMs: 400
			});
		}
		return () => {
			if (engine.music.contextId !== "title-screen") return;
			engine.music.contextId = void 0;
			engine.music.leave({
				exitDelayMs: 0,
				fadeOutMs: 400
			});
		};
	});
	return h(DOMContainer, {
		width: "100%",
		height: "100%"
	}, h(DOMElement, {
		element: "div",
		attrs: {
			class: "rpg-ui-title-screen rpg-anim-fade-in",
			style: computed(() => ({ backgroundImage: backgroundImageStyle() }))
		}
	}, [
		h(DOMElement, {
			element: "div",
			attrs: { class: "rpg-ui-title-screen-header rpg-anim-float" }
		}, [h(DOMElement, {
			element: "div",
			attrs: { class: "rpg-ui-title-screen-title" },
			textContent: computed(() => titleText())
		}), cond(computed(() => subtitleText()), () => h(DOMElement, {
			element: "div",
			attrs: { class: "rpg-ui-title-screen-subtitle" },
			textContent: computed(() => subtitleText())
		}))]),
		h(Navigation, {
			tabindex: selectedEntry,
			controls
		}, h(DOMElement, {
			element: "div",
			attrs: { class: "rpg-ui-menu rpg-ui-title-screen-menu rpg-anim-slide-up" }
		}, loop(computed(() => entryList()), (entry, index) => h(DOMElement, {
			element: "div",
			attrs: {
				class: ["rpg-ui-menu-item", computed(() => ({ disabled: isEntryDisabled(entry) }))],
				"data-selected": computed(() => selectedEntry() === index ? "true" : "false"),
				tabindex: index,
				click: selectEntry(index)
			},
			textContent: entry.label
		})))),
		cond(computed(() => versionText()), () => h(DOMElement, {
			element: "div",
			attrs: { class: "rpg-ui-title-screen-version" },
			textContent: computed(() => versionText())
		}))
	]));
}
var __ce_component = component;
//#endregion
export { __ce_component as default };

//# sourceMappingURL=title-screen.ce.js.map