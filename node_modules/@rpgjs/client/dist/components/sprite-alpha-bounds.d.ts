export type SpriteAlphaBounds = {
    left: number;
    top: number;
    right: number;
    bottom: number;
    width: number;
    height: number;
};
/**
 * Resolve the dimensions of the original image rather than those of a cached
 * Pixi texture frame. Pixi can mutate a cached texture to the active
 * spritesheet frame while its source resource keeps the full image size.
 */
export declare const resolveTextureSourceDimensions: (value: unknown) => {
    width: number;
    height: number;
} | undefined;
export declare const loadCachedSpriteSheetAlphaBounds: (key: string, loader: () => Promise<Array<SpriteAlphaBounds | null> | null>) => Promise<Array<SpriteAlphaBounds | null> | null>;
export declare const scanSpriteSheetAlphaBounds: (pixels: Uint8ClampedArray, imageWidth: number, imageHeight: number, framesWidth: number, framesHeight: number) => Array<SpriteAlphaBounds | null>;
export declare const mergeSpriteAlphaBounds: (values: Array<SpriteAlphaBounds | null | undefined>) => SpriteAlphaBounds | undefined;
