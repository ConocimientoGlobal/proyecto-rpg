import { effect } from "canvasengine";
//#region src/components/current-player-controls.ts
function observeCurrentPlayerControls(isCurrentPlayer, controls, register) {
	return effect(() => {
		if (isCurrentPlayer()) register(controls);
	}).subscription;
}
//#endregion
export { observeCurrentPlayerControls };

//# sourceMappingURL=current-player-controls.js.map