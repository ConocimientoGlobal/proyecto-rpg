import { ControlsDirective } from 'canvasengine';
import { Subscription } from 'rxjs';
export declare function observeCurrentPlayerControls(isCurrentPlayer: () => boolean, controls: ControlsDirective, register: (controls: ControlsDirective) => void): Subscription;
