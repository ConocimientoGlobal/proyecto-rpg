import { inject } from "../../core/inject.js";
import { resolveDynamicValue } from "./parse-value.js";
import { RpgClientEngine } from "../../RpgClientEngine.js";
import { Sprite, computed, h, useDefineEmits, useDefineProps, useProps } from "canvasengine";
//#region src/components/dynamics/image.ce
function component($$props) {
	useProps($$props);
	const defineProps = useDefineProps($$props);
	useDefineEmits($$props);
	const { object, value } = defineProps();
	const sprite = object();
	const client = inject(RpgClientEngine);
	return h(Sprite, { sheet: computed(() => {
		const id = resolveDynamicValue(value?.(), sprite);
		if (!id) return null;
		return {
			definition: client.getSpriteSheet(id),
			playing: "default"
		};
	}) });
}
var __ce_component = component;
//#endregion
export { __ce_component as default };

//# sourceMappingURL=image.ce.js.map