//#region src/services/localPlayerSync.ts
var hasAuthoritativePredictionState = (ack) => {
	return typeof ack?.frame === "number" && Number.isFinite(ack.frame) && typeof ack.x === "number" && Number.isFinite(ack.x) && typeof ack.y === "number" && Number.isFinite(ack.y);
};
/**
* Keep a predicted local player's authoritative position out of the generic
* sync loader. The prediction controller must be the only code path that
* decides whether an authoritative position warrants a correction.
*/
var routePredictedLocalPlayerSync = (payload, playerId, currentState, ack) => {
	const players = payload?.players;
	const localPatch = playerId && players ? players[playerId] : void 0;
	const hasX = typeof localPatch?.x === "number" && Number.isFinite(localPatch.x);
	const hasY = typeof localPatch?.y === "number" && Number.isFinite(localPatch.y);
	const x = hasX ? localPatch.x : currentState?.x;
	const y = hasY ? localPatch.y : currentState?.y;
	if (!playerId || !localPatch || !hasX && !hasY || typeof x !== "number" || !Number.isFinite(x) || typeof y !== "number" || !Number.isFinite(y)) return { payload };
	const snapshot = {
		x,
		y,
		direction: localPatch.direction ?? currentState?.direction
	};
	const filteredLocalPatch = { ...localPatch };
	delete filteredLocalPatch.x;
	delete filteredLocalPatch.y;
	delete filteredLocalPatch.direction;
	delete filteredLocalPatch._frames;
	return {
		payload: {
			...payload,
			players: {
				...players,
				[playerId]: filteredLocalPatch
			}
		},
		snapshot: hasAuthoritativePredictionState(ack) ? void 0 : snapshot
	};
};
//#endregion
export { hasAuthoritativePredictionState, routePredictedLocalPlayerSync };

//# sourceMappingURL=localPlayerSync.js.map