import { PredictionState } from '@rpgjs/common';
export interface RoutedLocalPlayerSync<DirectionType> {
    payload: any;
    snapshot?: PredictionState<DirectionType>;
}
export interface PredictionAckLike {
    frame?: unknown;
    x?: unknown;
    y?: unknown;
}
export declare const hasAuthoritativePredictionState: (ack: PredictionAckLike | null | undefined) => boolean;
/**
 * Keep a predicted local player's authoritative position out of the generic
 * sync loader. The prediction controller must be the only code path that
 * decides whether an authoritative position warrants a correction.
 */
export declare const routePredictedLocalPlayerSync: <DirectionType>(payload: any, playerId: string | null | undefined, currentState?: PredictionState<DirectionType>, ack?: PredictionAckLike | null) => RoutedLocalPlayerSync<DirectionType>;
