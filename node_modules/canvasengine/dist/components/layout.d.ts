import { Props } from '../engine/reactive';
import { LayoutBorder, Size } from './types/DisplayObject';
export type LayoutStyle = Record<string, unknown>;
export declare function isLayoutBorder(value: unknown): value is LayoutBorder;
export declare function normalizeLayoutProps(props: Props, options?: {
    containerAnchor?: boolean;
}): LayoutStyle;
export declare function hasLayoutContainerProps(props: Props): boolean;
export declare function hasLayoutNodeProps(props: Props): boolean;
/**
 * Returns true when a node uses layout values that cannot be resolved without
 * a Yoga box on its direct parent.
 *
 * A fixed-size flex container can be an independent Yoga root. Percentages,
 * trailing insets and flex-item properties, however, are relative to a parent.
 */
export declare function requiresLayoutParent(props: Props): boolean;
export declare function withLayoutSize(props: Props, width: Size, height: Size): Props;
//# sourceMappingURL=layout.d.ts.map