{
  "install": "npm install",
  "watch": {
    "install": {
      "include": ["^package\\.json$"],
      "restart": true
    },
    "restart": {
      "exclude": [
        "^public/",
        "^dist/"
      ]
    }
  }
}
